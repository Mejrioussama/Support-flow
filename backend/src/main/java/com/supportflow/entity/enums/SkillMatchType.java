package com.supportflow.entity.enums;

/**
 * How well an agent skill profile matches the ticket category.
 */
public enum SkillMatchType {
    PRIMARY,
    SECONDARY,
    FALLBACK,
    MANAGER_FALLBACK
}
