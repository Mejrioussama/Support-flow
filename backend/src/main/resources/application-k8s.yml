# ===============================================
# SupportFlow - Kubernetes Profile Configuration
# ===============================================

server:
  port: 8080

management:
  health:
    camunda:
      enabled: false
    mail:
      enabled: false
  endpoint:
    health:
      show-details: always
